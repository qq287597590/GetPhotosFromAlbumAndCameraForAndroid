<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
    </style>
    <style name="TopLayoutDefaultStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/actionbar_statusbar_height</item>
        <item name="android:background">@color/tablayout_bg_color</item>
        <item name="android:minHeight">@dimen/actionbar_statusbar_height</item>
        <!--<item name="android:paddingTop">@dimen/statusbar_height</item>-->
        <item name="android:gravity">center_vertical</item>
    </style>
</resources>
